import { WindowFrameName } from '@portal-windows/core'

export const MAIN_FRAME_NAME = 'main_window' as WindowFrameName
export const SIMPLE_MIRROR_WINDOW = 'simple_mirror_window' as WindowFrameName
export const ROTATING_MIRROR_WINDOW = 'rotating_mirror_window' as WindowFrameName
export const NESTED_ROTATING_MIRROR_WINDOW = 'nested_rotating_mirror_window' as WindowFrameName
